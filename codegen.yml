overwrite: true
watch: true
schema: 'src/graphql/**/*.ts'
generates:
  src/generated/types.d.ts:
    config:
      contextType: ../config/context#Context
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
