overwrite: true
watch: true
schema: 'src/schema/**/*.ts'
generates:
  src/generated/types.ts:
    config:
      contextType: ../config/context#Context
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
